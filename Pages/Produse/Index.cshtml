@page
@model Pecolzz.Pages.Produse.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Denumire_produs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Descriere)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Pret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].DataAdaugarii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produs[0].Cod)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Produs)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Denumire_produs)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descriere)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pret)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataAdaugarii)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cod.CodArticol)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
public IndexModel(Pecolzz.Data.PecolzzContext context)
{
_context = context;
}
public IList<Produs> Produs { get;set; } = default!;
    public ProdusData ProdusID { get; set; }
    public int ProdusID { get; set; }
    public int CategorieID { get; set; }

    public string Denumire_produsSort { get; set; } 
    public decimal PretSort { get; set; }
   
